# ClickClip 기능 명세서

## 개요

영상 편집 과정을 단계별(Step-by-Step)로 수행하는 프론트엔드 중심 워크플로우.
사용자는 대본 입력부터 장면 구성, TTS/이미지 생성을 거쳐 최종적으로 CapCut 프로젝트 파일로 다운로드한다.

> 본 문서는 **프론트 단에서 수행되는 기능**을 중심으로 정리한다.
> (대본을 장면별로 나누는 로직 등 일부 백엔드 처리 과정은 추후 별도 정리)

---

## 워크플로우 요약

| 단계 | 명칭 | 핵심 내용 |
|------|------|-----------|
| 1단계 | 대본 입력 | 대본을 직접 입력 |
| 2단계 | 장면 구성 | SRT·TTS 생성, 장면별 이미지 선정 |
| 최종 | 다운로드 | CapCut 구조에 맞춘 에셋·프로젝트 파일 다운로드 |

---

## 1단계: 대본 입력

대본을 직접 입력하는 화면.

### 기능 요구사항
- **직접 입력**: 사용자가 대본 텍스트를 직접 입력할 수 있어야 한다.
- **입력 영역(Textarea)**: 입력 칸이 충분히 커야 한다. (여러 문단의 대본을 편하게 작성/확인 가능)
- **계속하기 버튼**: 다음 단계(2단계)로 진행하는 버튼이 존재해야 한다.

### 비고
- 이 단계는 전적으로 프론트 단에서 수행된다.

---

## 2단계: 장면 구성 (SRT · TTS 생성, 장면별 이미지 선정)

입력된 대본을 **장면(Scene) 단위**로 나누어 편집하는 단계.
> 대본을 장면별로 나누는 과정은 추후 별도 정리.

### 2-1. 장면(Scene) 구조

각 장면은 다음 요소로 구성된다.

| 구성 요소 | 설명 |
|-----------|------|
| 이미지 | 장면을 대표하는 이미지/GIF (업로드·추천·AI 생성 중 선택) |
| 장면 번호 | 장면의 순서 번호 |
| 자막 | 한 장면에 **여러 개의 자막**이 존재 가능 |

### 2-2. 장면 편집 기능
- **장면 추가**: 새로운 장면을 추가할 수 있다.
- **자막 편집**: 장면 내에서 텍스트(자막)를 **추가 / 삭제 / 수정**할 수 있다.

### 2-3. TTS 생성
- 장면 자막 기반으로 **TTS 생성** 버튼이 존재한다.
- 생성된 TTS를 **듣기(재생)** 할 수 있는 버튼이 존재한다.

### 2-4. 이미지 선정

장면의 이미지 칸을 선택하면 아래 방식 중에서 이미지를 선택할 수 있다.

#### (1) 직접 업로드
- 사용자가 직접 이미지를 업로드한다.

#### (2) 추천 GIF / 이미지 중 선택
- **추천 GIF**: Giphy API 기반 추천. 각 장면별 **자막 키워드**를 기반으로 추천한다.
- **추천 이미지**: Google 검색 API 기반 추천.

#### (3) AI 이미지 생성
- 장면 내용을 기반으로 **이미지 생성 프롬프트를 5개 정도 추천**한다.
- 사용자는 추천 프롬프트를 **선택**하거나 **직접 작성**할 수 있다.
- 선택/작성한 프롬프트로 이미지 생성 API를 호출한다.

---

## 최종: 다운로드

CapCut 파일 구조에 맞게 편집 결과물을 다운로드한다.

### 다운로드 포함 항목
- **SRT**: 자막 파일
- **TTS (mp3)**: 장면별 음성 파일
- **이미지 에셋**: 장면별 이미지/GIF
- **프로젝트 파일**: CapCut 프로젝트 구조에 맞춘 파일

### 요구사항
- 위 항목들이 CapCut에서 바로 불러올 수 있는 구조로 패키징되어야 한다.

---

## 외부 API 연동 정리

| 용도 | API |
|------|-----|
| GIF 추천 | Giphy API |
| 이미지 추천 | Google 검색 API |
| AI 이미지 생성 | 이미지 생성 API |
| TTS 생성 | TTS API |

---

## 추후 정리 항목 (TBD)
- 대본을 장면별로 나누는 로직
- 백엔드 처리 과정 상세
- TTS / 이미지 생성 / 추천 API의 구체적 스펙 및 파라미터
- CapCut 프로젝트 파일 포맷 상세 구조
