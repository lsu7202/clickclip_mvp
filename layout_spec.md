# ClickClip 프론트엔드 레이아웃 명세서

와이어프레임(`input.png`, `playground.png`, `AI_image.png`) 기반으로 각 화면의 레이아웃을 정리한다.
기능 정의는 [spec.md](spec.md) 참조. 본 문서는 **화면 구성 / 배치**에 집중한다.

---

## 화면 목록

| 화면 | 와이어프레임 | 대응 단계 |
|------|--------------|-----------|
| 대본 입력 화면 | `input.png` | 1단계 |
| 장면 편집(Playground) 화면 | `playground.png` | 2단계 |
| AI 이미지 생성 패널 | `AI_image.png` | 2단계 - 우측 패널 탭 |

---

## 1. 대본 입력 화면 (`input.png`)

1단계 대본 직접 입력 화면. 단일 입력에 집중한 미니멀 레이아웃.

```
┌────────────────────────────────────────────────────┐
│                                                    │
│              ┌──────────────────────┐              │
│              │                      │              │
│              │                      │              │
│              │       대본            │              │
│              │     직접입력          │              │
│              │   (Textarea)         │              │
│              │                      │              │
│              │                      │              │
│              └──────────────────────┘              │
│                                                    │
│                                       [ 계속하기 ]  │
└────────────────────────────────────────────────────┘
```

### 구성 요소
| 요소 | 위치 | 설명 |
|------|------|------|
| 대본 입력 영역 (Textarea) | 화면 중앙 | 세로로 긴 대형 입력 박스. 여러 문단 작성 가능 |
| 계속하기 버튼 | 우측 하단 | 2단계로 진행. 입력 영역과 분리 배치 |

### 레이아웃 규칙
- 입력 영역은 화면 중앙에 배치하고 충분한 높이/너비를 확보한다.
- 계속하기 버튼은 우측 하단 고정 위치(다음 단계 진행 = 우하단 컨벤션).
- 주변 여백을 넓게 두어 입력에만 집중하도록 한다.

---

## 2. 장면 편집(Playground) 화면 (`playground.png`)

2단계 핵심 화면. 좌측 미리보기 · 중앙 장면 리스트 · 우측 에셋 선택 패널의 **3-컬럼 레이아웃**.

```
┌──────────────┬───────────────────────────┬─────────────────────────┐
│              │           [tts 생성][tts 듣기]│ ┌gif┬─이미지─┬─AI이미지─┐│
│              │  ┌──┐ 장면1  너네 아직도        │ │                       ││
│   미리보기     │  │썸│       아무 땅콩버터…     │ │ [img][img][img]       ││
│   /플레이어    │  └──┘                       │ │                       ││
│   (Preview)  │  ┌──┐ 장면2  천조국 헬창…       │ │ [img][img]            ││
│              │  │썸│       필수 식단…         │ │                       ││
│              │  └──┘       한국 제품이…        │ │ (그리드 썸네일)         ││
│              │  ┌──┐ 장면2  그건 바로 OOO임     │ │                       ││
│              │  │썸│       이게 왜…            │ │                       ││
│              │  └──┘   [＋장면추가]   [다운로드] │ │                       ││
└──────────────┴───────────────────────────┴─────────────────────────┘
   좌측 컬럼            중앙 컬럼                      우측 컬럼

   ※ 자막 줄 우측: [✎ 수정] [🗑 삭제]  /  장면 행 우측 상단(hover·선택 시): [🗑 장면 삭제]
   ※ 각 장면 하단: [＋ 자막 추가]  /  리스트 맨 하단: [＋ 장면 추가]
```

### 2-1. 좌측 컬럼 — 미리보기 / 플레이어
| 요소 | 설명 |
|------|------|
| Preview 영역 | 선택된 장면 또는 전체 영상 미리보기. 세로형 대형 박스 (숏폼 비율 가정) |

### 2-2. 중앙 컬럼 — 장면 리스트
세로 스크롤되는 장면(Scene) 리스트.

| 요소 | 위치 | 설명 |
|------|------|------|
| tts 생성 버튼 | 상단 우측 | 자막 기반 TTS 생성 |
| tts 듣기 버튼 | 상단 우측 (생성 옆) | 생성된 TTS 재생 |
| 장면 행(Scene Row) | 리스트 본문 | `[썸네일 이미지] + [장면 번호] + [자막들]` 가로 배치 |
| ┗ 썸네일 | 행 좌측 | 장면 대표 이미지. 클릭 시 우측 패널과 연동(에셋 선택) |
| ┗ 장면 번호 | 썸네일 우측 | "장면 1", "장면 2" … |
| ┗ 자막 영역 | 번호 우측 | 한 장면에 여러 줄 자막. 추가/삭제/수정 가능 |
| ┗ 자막 수정 버튼 (✎) | 각 자막 줄 우측 | 해당 자막 줄을 인라인 편집(클릭 시 편집 모드) |
| ┗ 자막 삭제 버튼 (🗑) | 각 자막 줄 우측 (수정 옆) | 해당 자막 줄 삭제 |
| ┗ 자막 추가 버튼 (＋ 자막 추가) | 각 장면 행 하단 | 해당 장면에 새 자막 줄 추가 |
| 장면 삭제 버튼 (🗑 장면 삭제) | 각 장면 행 우측 상단 | hover 또는 장면 선택 시 노출. 해당 장면 삭제 |
| 장면 추가 버튼 (＋ 장면 추가) | 중앙 컬럼 하단 좌측 | 리스트 맨 아래에 새 장면 추가 |
| 다운로드 버튼 | 중앙 컬럼 하단 우측 | CapCut 구조 패키지 다운로드 |

### 2-3. 우측 컬럼 — 에셋 선택 패널 (탭형)
| 탭 | 소스 | 설명 |
|------|------|------|
| gif | Giphy API | 자막 키워드 기반 GIF 추천 (기본 활성 탭) |
| 이미지 | Google 검색 API | 키워드 기반 이미지 추천 |
| AI 이미지 | 이미지 생성 API | AI 이미지 생성 (→ 3. 패널 참조) |

- 탭 하단은 추천 결과 **썸네일 그리드** (gif/이미지 탭 공통).
- 그리드 아이템 클릭 시 현재 선택된 장면의 이미지로 적용.
- (업로드 기능은 패널 또는 썸네일 클릭 흐름에 포함 — `spec.md` 참조)

### 레이아웃 규칙
- 3컬럼 비율 가이드: 좌측(미리보기) : 중앙(장면 리스트) : 우측(에셋) ≈ 약 3 : 4 : 3.
- 우측 패널은 중앙 리스트에서 선택된 장면과 상태를 공유한다(선택 장면 → 추천/적용 대상).
- tts 버튼군은 중앙 컬럼 상단 우측에 고정, 다운로드 버튼은 하단 우측에 고정.

---

## 3. AI 이미지 생성 패널 (`AI_image.png`)

우측 에셋 패널의 **"AI 이미지" 탭** 활성 상태. 프롬프트 추천 + 직접 입력 + 생성으로 구성.

```
┌─gif──┬─이미지─┬─[AI 이미지]─┐   ← 탭 (AI 이미지 활성)
├──────┴───────┴────────────┤
│  ┌──────────────────────┐  │
│  │   맛없는 땅콩버터        │  │  ← 추천 프롬프트 1
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ 트럼프가 땅콩버터를…     │  │  ← 추천 프롬프트 2
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ 페페가 땅콩버터를…       │  │  ← 추천 프롬프트 3
│  └──────────────────────┘  │
│            …               │  (5개 정도 추천)
│  ┌──────────────┬───────┐  │
│  │ (직접 입력)    │ [생성] │  │  ← 프롬프트 입력 + 생성 버튼
│  └──────────────┴───────┘  │
└────────────────────────────┘
```

### 구성 요소
| 요소 | 위치 | 설명 |
|------|------|------|
| 탭 헤더 | 상단 | gif / 이미지 / AI 이미지 (AI 이미지 활성) |
| 추천 프롬프트 리스트 | 본문 | 장면 내용 기반 추천 프롬프트 **5개 정도**. 각 항목 클릭 시 선택/입력칸에 반영 |
| 프롬프트 입력칸 | 하단 좌측 | 직접 프롬프트 작성 가능 |
| 생성 버튼 | 하단 우측 | 선택/작성한 프롬프트로 이미지 생성 API 호출 |

### 레이아웃 규칙
- 추천 프롬프트는 세로로 나열된 클릭 가능한 박스(리스트) 형태.
- 입력칸 + 생성 버튼은 패널 하단에 가로로 고정 배치.
- 생성된 이미지는 현재 선택된 장면에 적용(중앙 리스트와 연동).

---

## 공통 레이아웃 / 상태 연동 정리

- **단계 전환**: 1단계(`input.png`) → 계속하기 → 2단계(`playground.png`).
- **선택 장면 컨텍스트**: 2단계에서 중앙 리스트의 "선택된 장면"이 우측 패널(추천/적용/AI 생성)의 작업 대상이 된다.
- **버튼 위치 컨벤션**: 진행/완료성 버튼(계속하기·다운로드·생성)은 해당 영역의 **우측 하단**에 배치.
- **반응 영역**: 좌측 미리보기는 고정 비율, 중앙·우측은 콘텐츠에 따라 세로 스크롤.

---

## 추후 정리 항목 (TBD)
- 정확한 컬럼 px/비율, 반응형 브레이크포인트
- 이미지 업로드 진입 UI 위치 상세
- 자막 수정 인터랙션 상세(인라인 편집 vs 모달)
- 장면 순서 변경(드래그 등) 지원 여부
- 로딩/생성 중 상태 표시(TTS·이미지 생성 진행 표시)
